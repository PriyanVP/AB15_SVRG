PC environment:
    To install all required modules for HWitL testing with pyTest, run this command:
    pip install -r .\firmware\AB15_MCU.Tests\environment_dependensies.txtenvironment_dependensies.txt

HW preparation:
    Before running tests, make sure that:
        1) ShieldBuddy is powered up and connected to PC;
        2) AB15(12) is powered and connected to ShieldBuddy;
        3) Adequate firmware is flashed into ShieldBuddy.