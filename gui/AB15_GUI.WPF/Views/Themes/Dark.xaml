<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--Image source-->
    <Image x:Key="FontSizeDecreaseIco" Source="../Resources/font_decrease_dark.png"/>
    <Image x:Key="FontSizeIncreaseIco" Source="../Resources/font_increase_dark.png"/>
    <Image x:Key="AscendingSortIco" Source="../Resources/sort_ascending_dark.png"/>
    <Image x:Key="DescendingSortIco" Source="../Resources/sort_descending_dark.png"/>

    <BitmapImage x:Key="Ico.Home" UriSource="../Resources/home_dark.png"/>
    <BitmapImage x:Key="Ico.Config" UriSource="../Resources/config_dark.png"/>
    <BitmapImage x:Key="Ico.Sensor" UriSource="../Resources/sensor_dark.png"/>
    <BitmapImage x:Key="Ico.Watchdog" UriSource="../Resources/watchdog_dark.png"/>
    <BitmapImage x:Key="Ico.Airbag" UriSource="../Resources/airbag_dark.png"/>
    <BitmapImage x:Key="Ico.Chip" UriSource="../Resources/chip_dark.png"/>
    <BitmapImage x:Key="Ico.Menu" UriSource="../Resources/menu_dark.png"/>
    <BitmapImage x:Key="Ico.Zoom" UriSource="../Resources/zoom_dark.png"/>
    <BitmapImage x:Key="Ico.Arrow.Up" UriSource="../Resources/align_up_arrow_dark.png"/>
    <BitmapImage x:Key="Ico.Arrow.Down" UriSource="../Resources/align_down_arrow_dark.png"/>
    <BitmapImage x:Key="Ico.Configuration" UriSource="../Resources/configuration_dark.png"/>
    
    <!--All colors defines-->
    <SolidColorBrush x:Key="UI.BackGround" Color="#e4000000"/>

    <SolidColorBrush x:Key="TopPanel.Background" Color="#141414"/>

    <SolidColorBrush x:Key="BottonPanel.Background" Color="#141414"/>

    <SolidColorBrush x:Key="Menu.Background" Color="#141414"/>
    <SolidColorBrush x:Key="Menu.SeparateLine" Color="#282828"/>

    <SolidColorBrush x:Key="UI.SeparateLine" Color="#f8ffffff"/>

    <SolidColorBrush x:Key="UI.TextBoxBackground" Color="#ff404040"/>
    
    <SolidColorBrush x:Key="ToolTipError.Background" Color="#ff1e1e"/>
    <SolidColorBrush x:Key="ToolTipError.BorderBrush" Color="#f8ffffff"/>

    <SolidColorBrush x:Key="Button.Background.StandBy" Color="Transparent"/>
    <SolidColorBrush x:Key="Button.Background.IsMouseOver" Color="#ff353535"/>
    <SolidColorBrush x:Key="Button.Background.IsPressed" Color="#5effffff"/>

    <SolidColorBrush x:Key="SimpleButton.Background.StandBy" Color="Transparent"/>
    <SolidColorBrush x:Key="SimpleButton.Background.IsMouseOver" Color="#ff353535"/>
    <SolidColorBrush x:Key="SimpleButton.Background.IsPressed" Color="#5effffff"/>
    <SolidColorBrush x:Key="SimpleButton.Background.Disable" Color="#c0c0c0c0"/>
    <SolidColorBrush x:Key="SimpleButton.BorderBrush" Color="#ff404040"/>
    <SolidColorBrush x:Key="SimpleButton.ValidationError" Color="#ff1e1e"/>
    
    <SolidColorBrush x:Key="Foreground.Helper" Color="#c0c0c0c0"/>
    <SolidColorBrush x:Key="Foreground.Disable" Color="#8a8886"/>
    <SolidColorBrush x:Key="Foreground.Default" Color="#E1E1E1"/>
    <SolidColorBrush x:Key="Foreground.Trace" Color="#C0C0C0"/>
    <SolidColorBrush x:Key="Foreground.Debug" Color="#50d050"/>
    <SolidColorBrush x:Key="Foreground.Info" Color="#28A828"/>
    <SolidColorBrush x:Key="Foreground.Warn" Color="#FFC31E"/>
    <SolidColorBrush x:Key="Foreground.Error" Color="#ff1e1e"/>
    <SolidColorBrush x:Key="Foreground.Fatal" Color="#f50000"/>
    <SolidColorBrush x:Key="Foreground.Red.Default" Color="#eb0000"/>
    <SolidColorBrush x:Key="Foreground.Red.Disable" Color="#af0000"/>

    <SolidColorBrush x:Key="CheckBox.Checked.Background" Color="#ff404040" />
    <SolidColorBrush x:Key="CheckBox.Checked.BorderBrush" Color="#edeced" />
    <SolidColorBrush x:Key="CheckBox.Checked.Fill" Color="#f8ffffff" />
    <SolidColorBrush x:Key="CheckBox.Unchecked.Background" Color="#f8ffffff" />
    <SolidColorBrush x:Key="CheckBox.Unchecked.BorderBrush" Color="#ff404040" />
    <SolidColorBrush x:Key="CheckBox.Unchecked.Fill" Color="#e4000000" />

    <SolidColorBrush x:Key="TextBox.Static.Backgroung" Color="#ff404040" />
    <SolidColorBrush x:Key="TextBox.Static.Border" Color="#7F7A7A7A" />
    <SolidColorBrush x:Key="TextBox.MouseOver.Border" Color="#f8ffffff" />
    <SolidColorBrush x:Key="TextBox.Focus.Border" Color="#edeced" />

    <SolidColorBrush x:Key="StatusBox.NotConnected" Color="#e4000000"/>
    <SolidColorBrush x:Key="StatusBox.Connected" Color="#006c00"/>
    <SolidColorBrush x:Key="StatusBox.Error" Color="#af0000"/>
    <SolidColorBrush x:Key="StatusBox.Warning" Color="#cd7300"/>
    <SolidColorBrush x:Key="StatusBox.NoStatus" Color="#e4000000"/>
    <SolidColorBrush x:Key="StatusBox.Good" Color="#006c00"/>
    <SolidColorBrush x:Key="StatusBox.Fault" Color="#af0000"/>

    <SolidColorBrush x:Key="Animation.Arrow" Color="#50d050"/>
    <SolidColorBrush x:Key="Animation.Lines" Color="#f8ffffff"/>

    <SolidColorBrush x:Key="Slider.ResponseTime.Line" Color="Gray"/>
    <SolidColorBrush x:Key="Slider.ResponseTime.FillColor" Color="#28A828"/>
    <SolidColorBrush x:Key="Slider.ResponseTime.Arrow" Color="#c0c0c0c0"/>

    <SolidColorBrush x:Key="Slider.LockTime.Line" Color="Transparent"/>
    <SolidColorBrush x:Key="Slider.LockTime.FillColor" Color="#f50000"/>
    <SolidColorBrush x:Key="Slider.LockTime.Arrow" Color="#c0c0c0c0"/>

    <SolidColorBrush x:Key="Slider.DisableThreshold.Top.Line" Color="Gray"/>
    <SolidColorBrush x:Key="Slider.DisableThreshold.Top.FillColor" Color="#28A828"/>
    <SolidColorBrush x:Key="Slider.DisableThreshold.Top.Arrow" Color="#c0c0c0c0"/>

    <SolidColorBrush x:Key="Slider.DisableThreshold.Bot.Line" Color="Gray"/>
    <SolidColorBrush x:Key="Slider.DisableThreshold.Bot.FillColor" Color="#FFA500"/>
    <SolidColorBrush x:Key="Slider.DisableThreshold.Bot.Arrow" Color="#c0c0c0c0"/>

    <SolidColorBrush x:Key="ListView.Background" Color="Transparent" />

    <SolidColorBrush x:Key="ListViewItem.Background" Color="Transparent" />

    <BooleanToVisibilityConverter x:Key="btv" />

    <!--#region ToolTip -->
    <Style TargetType="ToolTip">
        <Setter Property="Foreground" Value="{DynamicResource Foreground.Default}"/>
        <Setter Property="Background" Value="{DynamicResource UI.BackGround}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource UI.SeparateLine}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>
    
    <Style x:Key="ToolTipError" TargetType="ToolTip">
        <Setter Property="Foreground" Value="{DynamicResource Foreground.Default}"/>
        <Setter Property="Background" Value="{DynamicResource ToolTipError.Background}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource ToolTipError.BorderBrush}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>
    <!--#endregion-->
    
    <!--#region SimpleToggleButton -->
    <Style x:Key="SimpleToggleButton" TargetType="ToggleButton">

        <Setter Property="Background" Value="{DynamicResource Button.Background.StandBy}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="{DynamicResource Foreground.Default}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border Background="{TemplateBinding Background}" 
                        BorderThickness="1"
                        BorderBrush="{DynamicResource SimpleButton.BorderBrush}">
                        <ContentPresenter HorizontalAlignment="Center"                  
                                      VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Button.Background.IsMouseOver}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource Button.Background.IsPressed}"/>
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
                <Setter Property="Background" Value="{DynamicResource SimpleButton.Background.IsMouseOver}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    <!--#endregion-->
    
    <!--#region AdditionalInfoImageStyle style define -->
    <Style x:Key="AdditionalInfoImageStyle" TargetType="{x:Type Image}">
        <Setter Property="Source" Value="{DynamicResource Ico.Arrow.Down}"/>

        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="True">
                <Setter Property="Source" Value="{DynamicResource Ico.Arrow.Up}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    <!--#endregion-->

    <!--#region Default Button style define -->
    <Style TargetType="{x:Type Button}" x:Key="DefaultButton">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="Background" Value="{DynamicResource Button.Background.StandBy}"/>
        <Setter Property="Foreground" Value="{DynamicResource Foreground.Default}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Margin" Value="1,2,1,2"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Button.Background.IsMouseOver}"/>
                <Setter Property="Cursor" Value="Hand"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource Button.Background.IsPressed}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    <!--#endregion-->
    
    <!--#region TextBlock -->
    <Style TargetType="TextBlock" x:Key="CustomTextBlock">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="Foreground" Value="{DynamicResource Foreground.Default}"/>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource Foreground.Disable}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="TextBlock" x:Key="CustomeRedTextBlock">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="Foreground" Value="{DynamicResource Foreground.Red.Default}"/>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource Foreground.Red.Disable}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    <!--#endregion-->

    <!--#region Simple button -->
    <Style TargetType="{x:Type Button}" x:Key="SimpleButton">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="Background" Value="{DynamicResource SimpleButton.Background.StandBy}"/>
        <Setter Property="Foreground" Value="{DynamicResource Foreground.Default}"/>
        <Setter Property="Margin" Value="1,2,1,2"/>
        <Setter Property="Padding" Value="2"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderThickness="1" BorderBrush="{DynamicResource SimpleButton.BorderBrush}">
                        <ContentPresenter Margin="2" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Button.Background.IsMouseOver}"/>
                <Setter Property="Cursor" Value="Hand"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource Button.Background.IsPressed}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource SimpleButton.Background.Disable}"/>
            </Trigger>
            <Trigger Property="Validation.HasError" Value="True">
                <Setter Property="ToolTip">
                    <Setter.Value>
                        <ToolTip DataContext="{Binding RelativeSource={RelativeSource Self}, Path=PlacementTarget}">
                            <ItemsControl ItemsSource="{Binding Path=(Validation.Errors)}" DisplayMemberPath="ErrorContent" />

                        </ToolTip>
                    </Setter.Value>
                </Setter>
            </Trigger>
            <Trigger Property="IsVisible" Value="true">
                <Setter Property="Validation.ErrorTemplate">
                    <Setter.Value>
                        <ControlTemplate>
                            <Border BorderBrush="{DynamicResource SimpleButton.ValidationError}" BorderThickness="3">
                                <AdornedElementPlaceholder/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>
    <!--#endregion-->

    <!--#region Top responce time slider-->

    <Style x:Key="SliderRepeatButton" TargetType="RepeatButton">
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="IsTabStop" Value="false" />
        <Setter Property="Focusable" Value="false" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border  Background="{DynamicResource Slider.ResponseTime.Line}" 
                             Height="5" BorderBrush="Transparent" BorderThickness="0,2,0,2"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SliderRepeatButton1" TargetType="RepeatButton">
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border SnapsToDevicePixels="True" Background="Gray"  BorderThickness="0,2,0,2" 
                            BorderBrush="{DynamicResource Slider.ResponseTime.FillColor}" Height="5"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SliderThumb" TargetType="Thumb">
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Canvas Background="Transparent">
                        <Polygon Points="0,5 10,-10 -10,-10" Fill="{DynamicResource Slider.ResponseTime.Arrow}"></Polygon>
                    </Canvas>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="WatchdogTopSlider"  TargetType="Slider">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" MinHeight="{TemplateBinding MinHeight}" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Track Grid.Row="1" x:Name="PART_Track"   >
                <Track.DecreaseRepeatButton>
                    <RepeatButton Style="{StaticResource SliderRepeatButton1}"  Command="Slider.DecreaseLarge" />
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Style="{StaticResource SliderThumb}"  />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Style="{StaticResource SliderRepeatButton}" Command="Slider.IncreaseLarge" />
                </Track.IncreaseRepeatButton>
            </Track>
        </Grid>
    </ControlTemplate>

    <Style x:Key="WatchdogTop_Slider" TargetType="Slider">
        <Setter Property="Focusable" Value="True"/>
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="MinHeight" Value="21" />
                <Setter Property="MinWidth" Value="104" />
                <Setter Property="Template" Value="{StaticResource WatchdogTopSlider}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!--#endregion-->

    <!--#region Bot lock time slider-->

    <Style x:Key="SliderRepeatButtonBot" TargetType="RepeatButton">
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="IsTabStop" Value="false" />
        <Setter Property="Focusable" Value="false" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border  Background="{DynamicResource Slider.LockTime.Line}" Height="5" BorderBrush="Transparent" BorderThickness="0,2,0,2"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SliderRepeatButton1Bot" TargetType="RepeatButton">
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border SnapsToDevicePixels="True" Background="Gray"  BorderThickness="0,2,0,2" 
                            BorderBrush="{DynamicResource Slider.LockTime.FillColor}" Height="5"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SliderThumbBot" TargetType="Thumb">
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Canvas Background="Transparent">
                        <Polygon Points="0,15 -10,30 10,30" Fill="{DynamicResource Slider.LockTime.Arrow}"></Polygon>
                    </Canvas>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="WatchdogBotSlider"  TargetType="Slider">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" MinHeight="{TemplateBinding MinHeight}" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Track Grid.Row="1" x:Name="PART_Track"   >
                <Track.DecreaseRepeatButton>
                    <RepeatButton Style="{StaticResource SliderRepeatButton1Bot}"  Command="Slider.DecreaseLarge" />
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Style="{StaticResource SliderThumbBot}"  />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Style="{StaticResource SliderRepeatButtonBot}" Command="Slider.IncreaseLarge" />
                </Track.IncreaseRepeatButton>
            </Track>
        </Grid>
    </ControlTemplate>

    <Style x:Key="WatchdogBot_Slider" TargetType="Slider">
        <Setter Property="Focusable" Value="True"/>
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="MinHeight" Value="21" />
                <Setter Property="MinWidth" Value="104" />
                <Setter Property="Template" Value="{StaticResource WatchdogBotSlider}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!--#endregion-->

    <!--#region Top Threshold slider-->

    <Style x:Key="SliderRepeatButtonThresholdTop" TargetType="RepeatButton">
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="IsTabStop" Value="false" />
        <Setter Property="Focusable" Value="false" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border  Background="{DynamicResource Slider.DisableThreshold.Top.Line}" 
                             Height="5" BorderBrush="Transparent" BorderThickness="0,2,0,2"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SliderRepeatButton1ThresholdTop" TargetType="RepeatButton">
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border SnapsToDevicePixels="True" Background="Gray"  BorderThickness="0,2,0,2" 
                            BorderBrush="{DynamicResource Slider.DisableThreshold.Top.FillColor}" Height="5"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SliderThumbThresholdTop" TargetType="Thumb">
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Canvas Background="Transparent">
                        <Polygon Points="0,5 10,-10 -10,-10" 
                                 Fill="{DynamicResource Slider.DisableThreshold.Bot.Arrow}"></Polygon>
                    </Canvas>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="WatchdogSliderThresholdTop"  TargetType="Slider">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" MinHeight="{TemplateBinding MinHeight}" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Track Grid.Row="1" x:Name="PART_Track"   >
                <Track.DecreaseRepeatButton>
                    <RepeatButton Style="{StaticResource SliderRepeatButton1ThresholdTop}"  Command="Slider.DecreaseLarge" />
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Style="{StaticResource SliderThumbThresholdTop}"  />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Style="{StaticResource SliderRepeatButtonThresholdTop}" Command="Slider.IncreaseLarge" />
                </Track.IncreaseRepeatButton>
            </Track>
        </Grid>
    </ControlTemplate>

    <Style x:Key="WatchdogTop_SliderThresholdTop" TargetType="Slider">
        <Setter Property="Focusable" Value="True"/>
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="MinHeight" Value="21" />
                <Setter Property="MinWidth" Value="104" />
                <Setter Property="Template" Value="{StaticResource WatchdogSliderThresholdTop}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!--#endregion-->

    <!--#region Bot lock time slider-->

    <Style x:Key="SliderRepeatButtonThresholdBot" TargetType="RepeatButton">
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="IsTabStop" Value="false" />
        <Setter Property="Focusable" Value="false" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border  Background="{DynamicResource Slider.DisableThreshold.Bot.Line}" 
                             Height="5" BorderBrush="Transparent" BorderThickness="0,2,0,2"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SliderRepeatButton1ThresholdBot" TargetType="RepeatButton">
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border SnapsToDevicePixels="True" Background="Gray"  BorderThickness="0,2,0,2" 
                            BorderBrush="{DynamicResource Slider.DisableThreshold.Bot.FillColor}" Height="5"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SliderThumbThresholdBot" TargetType="Thumb">
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Canvas Background="Transparent">
                        <Polygon Points="0,15 -10,30 10,30" 
                                 Fill="{DynamicResource Slider.DisableThreshold.Bot.Arrow}"></Polygon>
                    </Canvas>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="WatchdogSliderThresholdBot"  TargetType="Slider">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" MinHeight="{TemplateBinding MinHeight}" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Track Grid.Row="1" x:Name="PART_Track"   >
                <Track.DecreaseRepeatButton>
                    <RepeatButton Style="{StaticResource SliderRepeatButton1ThresholdBot}"  Command="Slider.DecreaseLarge" />
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Style="{StaticResource SliderThumbThresholdBot}"  />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Style="{StaticResource SliderRepeatButtonThresholdBot}" Command="Slider.IncreaseLarge" />
                </Track.IncreaseRepeatButton>
            </Track>
        </Grid>
    </ControlTemplate>

    <Style x:Key="Watchdog_SliderThresholdBot" TargetType="Slider">
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="MinHeight" Value="21" />
                <Setter Property="MinWidth" Value="104" />
                <Setter Property="Template" Value="{StaticResource WatchdogSliderThresholdBot}" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <!--#endregion-->

    <!--#region style a top level menu item -->
    <ControlTemplate x:Key="VsMenuTop" TargetType="MenuItem">
        <StackPanel TextBlock.FontSize="20" Height="40">
            <!-- label, icons, etc. -->
            <Label Content="{TemplateBinding Header}" Foreground="{DynamicResource Foreground.Default}" />

            <!-- sub items -->
            <Popup IsOpen="{TemplateBinding IsSubmenuOpen}" AllowsTransparency="True" Focusable="False">
                <Border BorderThickness="1" BorderBrush="{DynamicResource UI.SeparateLine}" Background="{DynamicResource UI.BackGround}">
                    <StackPanel IsItemsHost="True" />
                </Border>
            </Popup>

            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver"  Value="True">
                            <Setter Property="Background" Value="{DynamicResource Button.Background.IsMouseOver}" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>
        </StackPanel>
    </ControlTemplate>
    <!--#endregion-->

    <!--#region style a non-top level menu item -->
    <ControlTemplate x:Key="MenuSubItem" TargetType="MenuItem">
        <DockPanel TextBlock.FontSize="15px" x:Name="panel">
            <!-- label, icons, etc. -->
            <Label Content="{TemplateBinding Header}" Foreground="{DynamicResource Foreground.Default}"/>
        </DockPanel>
        <ControlTemplate.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" TargetName="panel" Value="{DynamicResource Button.Background.IsMouseOver}"/>

            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <!--#endregion-->

    <!--#region Menu -->
    <Style TargetType="{x:Type Menu}">
        <Setter Property="Background" Value="{DynamicResource Button.Background.StandBy}" />
    </Style>

    <Style x:Key="MenuToggleButtonTemplate" TargetType="ToggleButton">

        <Setter Property="Background" Value="{DynamicResource Button.Background.StandBy}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border Background="{TemplateBinding Background}" 
                            BorderThickness="0"
                            BorderBrush="{DynamicResource TopPanel.Background}">
                        <ContentPresenter HorizontalAlignment="Left"                  
                                              VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Button.Background.IsMouseOver}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource Button.Background.IsPressed}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="MenuButtons" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource Button.Background.StandBy}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Margin" Value="0,1"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>


        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Button.Background.IsMouseOver}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource Button.Background.IsPressed}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="MenuToggleButtons" TargetType="ToggleButton">
        <Setter Property="Background" Value="{DynamicResource Button.Background.StandBy}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Margin" Value="0,1"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>


        <Style.Triggers>
            <Trigger Property="IsChecked" Value="True">
                <Setter Property="Background" Value="{DynamicResource Button.Background.IsMouseOver}"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Button.Background.IsMouseOver}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource Button.Background.IsPressed}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    <!--#endregion-->

    <!--#region Default TextBox style define -->
    <Style TargetType="{x:Type TextBox}">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="Foreground" Value="{DynamicResource Foreground.Default}"/>
        <Setter Property="Text" Value="20"/>
        <Setter Property="TextAlignment" Value="Center"/>
        <Setter Property="Margin" Value="1, 2, 1, 2"/>
        <Setter Property="Background" Value="{DynamicResource UI.TextBoxBackground}"/>
    </Style>
    <!--#endregion-->

    <!--#region Default Button style define -->
    <Style TargetType="{x:Type Button}">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="Background" Value="{DynamicResource Button.Background.StandBy}"/>
        <Setter Property="Foreground" Value="{DynamicResource Foreground.Default}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Margin" Value="1,2,1,2"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Button.Background.IsMouseOver}"/>
                <Setter Property="Cursor" Value="Hand"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource Button.Background.IsPressed}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    <!--#endregion-->

    <!--#region Default ToggleButton style define -->
    <Style x:Key="SortingToggleButtonStyle" TargetType="{x:Type ToggleButton}">
        <Setter Property="Background" Value="{DynamicResource Button.Background.StandBy}"/>
        <Setter Property="Foreground" Value="{DynamicResource Foreground.Default}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Margin" Value="1,2,1,2"/>

        <Setter Property="Content" Value="{DynamicResource AscendingSortIco}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource Button.Background.IsMouseOver}"/>
                <Setter Property="Cursor" Value="Hand"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{DynamicResource Button.Background.IsPressed}"/>
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
                <Setter Property="Content" Value="{DynamicResource DescendingSortIco}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    <!--#endregion-->

    <!--#region TextBlock style define -->
    <Style x:Key="TextBlockStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="Margin" Value="2,2"/>
        <Setter Property="Foreground" Value="{DynamicResource Foreground.Default}"/>
    </Style>
    <!--#endregion-->

    <!--#region Default ListView style define -->
    <Style TargetType="{x:Type ListView}">
        <Setter Property="Padding" Value="2,4,2,2"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
        <Setter Property="Background" Value="{DynamicResource ListView.Background}"/>
        <Setter Property="Foreground" Value="{DynamicResource Foreground.Default}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Hidden"/>
        <Setter Property="ScrollViewer.CanContentScroll" Value="False"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
    </Style>
    <!--#endregion-->

    <!--#region Default ListViewItem style define -->
    <Style x:Key="ListViewItemStyle" TargetType="{x:Type ListViewItem}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListViewItem">
                    <Border x:Name="border" Background="{DynamicResource ListViewItem.Background}">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="Disabled" />
                                <VisualState x:Name="MouseOver">
                                    <Storyboard>
                                        <ColorAnimationUsingKeyFrames Storyboard.TargetName="border"
                                                              Storyboard.TargetProperty="(Panel.Background).(SolidColorBrush.Color)">
                                            <!--Animation colors cannot be change dynamicly-->
                                            <EasingColorKeyFrame KeyTime="0" Value="#ff404040" />
                                        </ColorAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <ContentPresenter/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!--#endregion-->

    <!--#region Sliding toggle switch -->
    <Style x:Key="ToggleSwitchStyle" TargetType="CheckBox">
        <Setter Property="Height" Value="20" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CheckBox}">
                    <Grid Height="{TemplateBinding Height}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Viewbox Grid.Column="0" Stretch="Uniform" Height="{TemplateBinding Height}">
                            <Border x:Name="bk" 
                                    Background="{DynamicResource CheckBox.Unchecked.Background}"
                                    BorderBrush="{DynamicResource CheckBox.Unchecked.BorderBrush}"
                                    BorderThickness="1" CornerRadius="10" Width="35" Height="20">
                                <Ellipse x:Name="ep" Fill="{DynamicResource CheckBox.Unchecked.Fill}" 
                                                Margin="0,2,14,2" Width="14" Height="14" />
                            </Border>
                        </Viewbox>
                        <ContentPresenter x:Name="contentPresenter" Grid.Column="1"
                                        Focusable="False"
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        Margin="{TemplateBinding Padding}" RecognizesAccessKey="True"
                                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger  Property="IsEnabled" Value="false">
                            <Setter TargetName="contentPresenter" Property="IsEnabled" Value="False" />
                            <Setter TargetName="bk" Property="BorderBrush" Value="#554d56" />
                            <Setter TargetName="ep" Property="Fill" Value="#FFD5D5D5" />
                            <Setter Property="Background" TargetName="bk" Value="{DynamicResource CheckBox.Checked.Background}" />
                        </Trigger>

                        <Trigger Property="IsChecked" Value="true">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ParallelTimeline>
                                            <ThicknessAnimation Storyboard.TargetName="bk" Storyboard.TargetProperty="BorderThickness" From="1,1,1,1" To="0,0,0,0" Duration="0:0:0.125" AutoReverse="False" />
                                            <ThicknessAnimationUsingKeyFrames Storyboard.TargetName="ep" Storyboard.TargetProperty="Margin" BeginTime="00:00:00">
                                                <SplineThicknessKeyFrame KeyTime="00:00:00" Value="0,2,14,2" />
                                                <SplineThicknessKeyFrame KeyTime="00:00:00.125" Value="14,2,3,2" />
                                            </ThicknessAnimationUsingKeyFrames>
                                            <!--Fill animation-->
                                            <!--From CheckBox.Checked.Fill -> To CheckBox.UnChecked.Fill-->
                                            <ColorAnimation Storyboard.TargetName="bk" Storyboard.TargetProperty="Background.(SolidColorBrush.Color)"
                                        From="#f8ffffff"
                                        To="#e4000000"
                                        FillBehavior="Stop" 
                                        Duration="0:0:0.125"/>
                                            <!--BorderBrush animation-->
                                            <!--From CheckBox.Checked.BorderBrush -> To CheckBox.UnChecked.BorderBrush-->
                                            <ColorAnimation Storyboard.TargetName="bk" Storyboard.TargetProperty="BorderBrush.(SolidColorBrush.Color)"
                                        From="#edeced"
                                        To="#ff404040"
                                        FillBehavior="Stop" 
                                        Duration="0:0:0.125"/>
                                            <!--Background animation-->
                                            <!--From CheckBox.Checked.Background -> To CheckBox.UnChecked.Background-->
                                            <ColorAnimation Storyboard.TargetName="ep" Storyboard.TargetProperty="Fill.(SolidColorBrush.Color)"
                                        From="#ff404040"
                                        To="#f8ffffff"
                                        FillBehavior="Stop" 
                                        Duration="0:0:0.125"/>
                                        </ParallelTimeline>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ParallelTimeline>
                                            <ThicknessAnimation Storyboard.TargetName="bk" Storyboard.TargetProperty="BorderThickness" From="0,0,0,0" To="1,1,1,1" Duration="0:0:0.125" AutoReverse="False" />
                                            <ThicknessAnimationUsingKeyFrames Storyboard.TargetName="ep" Storyboard.TargetProperty="Margin" BeginTime="00:00:00">
                                                <SplineThicknessKeyFrame KeyTime="00:00:00" Value="14,2,0,2" />
                                                <SplineThicknessKeyFrame KeyTime="00:00:00.125" Value="0,2,14,2" />
                                            </ThicknessAnimationUsingKeyFrames>
                                            <ColorAnimation Storyboard.TargetName="bk" Storyboard.TargetProperty="Background.(SolidColorBrush.Color)"
                                        To="#f8ffffff"
                                        From="#e4000000"
                                        FillBehavior="Stop" 
                                        Duration="0:0:0.125"/>
                                            <!--Fill animation-->
                                            <!--From CheckBox.Checked.Fill <- To CheckBox.UnChecked.Fill-->
                                            <ColorAnimation Storyboard.TargetName="bk" Storyboard.TargetProperty="BorderBrush.(SolidColorBrush.Color)"
                                        To="#edeced"
                                        From="#ff404040"
                                        FillBehavior="Stop" 
                                        Duration="0:0:0.125"/>
                                            <!--BorderBrush animation-->
                                            <!--From CheckBox.Checked.BorderBrush <- To CheckBox.UnChecked.BorderBrush-->
                                            <ColorAnimation Storyboard.TargetName="ep" Storyboard.TargetProperty="Fill.(SolidColorBrush.Color)"
                                        To="#ff404040"
                                        From="#f8ffffff"
                                        FillBehavior="Stop" 
                                        Duration="0:0:0.0625"/>
                                            <!--Background animation-->
                                            <!--From CheckBox.Checked.Background <- To CheckBox.UnChecked.Background-->
                                        </ParallelTimeline>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                            <Setter TargetName="ep" Property="HorizontalAlignment" Value="Right" />
                        </Trigger>

                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="True" />
                                <Condition Property="IsEnabled" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter Property="Background" TargetName="bk" Value="{DynamicResource CheckBox.Checked.Background}" />
                            <Setter Property="BorderBrush" TargetName="bk" Value="{DynamicResource CheckBox.Checked.BorderBrush}" />
                            <Setter Property="Fill" TargetName="ep" Value="{DynamicResource CheckBox.Checked.Fill}" />
                        </MultiTrigger>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Cursor" Value="Hand"/>
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!--#endregion-->

    <!--#region TextBox -->
    <Style x:Key="TextBoxStyle" TargetType="{x:Type TextBox}">
        <Setter Property="Background" 
                    Value="{DynamicResource TextBox.Static.Backgroung}" />
        <Setter Property="BorderBrush" Value="{DynamicResource TextBox.Static.Border}" />
        <Setter Property="Foreground" 
                    Value="{DynamicResource Foreground.Default}" />
        <Setter Property="Margin" Value="2,2,2,2"/>
        <Setter Property="Padding" Value="8,4,4,8" />
        <Setter Property="BorderThickness" Value="1,1,1,2" />
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="KeyboardNavigation.TabNavigation" Value="None" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="AllowDrop" Value="true" />
        <Setter Property="ScrollViewer.PanningMode" Value="VerticalFirst" />
        <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Background="{TemplateBinding Background}" SnapsToDevicePixels="True"
                                CornerRadius="4">
                        <ScrollViewer x:Name="PART_ContentHost" Focusable="false"
                                    HorizontalScrollBarVisibility="Hidden"
                                    VerticalScrollBarVisibility="Hidden" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Opacity" TargetName="border" Value="0.56" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="true">
                            <Setter Property="Opacity" TargetName="border" Value="1" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="BorderBrush" TargetName="border"
                                        Value="{StaticResource TextBox.MouseOver.Border}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="true">
                            <Setter Property="BorderBrush" TargetName="border"
                                        Value="{StaticResource TextBox.Focus.Border}" />
                            <Setter Property="BorderThickness" TargetName="border" Value="1,1,1,2" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsInactiveSelectionHighlightEnabled" Value="true" />
                    <Condition Property="IsSelectionActive" Value="false" />
                </MultiTrigger.Conditions>
                <Setter Property="SelectionBrush"
                            Value="{DynamicResource {x:Static SystemColors.InactiveSelectionHighlightBrushKey}}" />
            </MultiTrigger>
        </Style.Triggers>
    </Style>
    <!--#endregion-->

    <Style TargetType="TabItem" x:Key="CustomTabItem">
        <Setter Property="Width" Value="Auto"/>
        <Setter Property="Background" Value="{DynamicResource UI.BackGround}" />
        <Setter Property="Height" Value="Auto"/>
        <Setter Property="Foreground" Value="{DynamicResource Foreground.Default}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border Name="Border" Background="Transparent">

                        <ContentPresenter x:Name="ContentSite"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Left"
                                        ContentSource="Header"
                                        Margin="10,3">
                        </ContentPresenter>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True" SourceName="Border">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource Button.Background.IsMouseOver}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource Button.Background.IsPressed}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>