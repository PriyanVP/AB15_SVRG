<Page x:Class="AB15_GUI.WPF.Views.WatchDog"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AB15_GUI.WPF.Views"
      xmlns:custom="clr-namespace:AB15_GUI.WPF.Views.Components"
      mc:Ignorable="d" 
      
      d:DesignHeight="450" d:DesignWidth="800"
      Title="WatchDog">

    <Grid Background="{DynamicResource UI.BackGround}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" MinHeight="10"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="{DynamicResource UI.SeparateLine}" BorderThickness="0, 0, 0, 1"/>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <Button Margin="3,3,3,3" Content="Status"/>
            <Button Content="Settings"/>
        </StackPanel>

        <custom:WatchdogSlider Grid.Row="1"></custom:WatchdogSlider>
    </Grid>
</Page>
